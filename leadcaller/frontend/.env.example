# Base URL of the backend API.
# - Local dev: leave empty (Vite proxy handles /admin, /health, /whatsapp, /webhooks).
# - Vercel deploy: set to your ngrok tunnel URL pointing at the laptop backend,
#   e.g. https://your-tunnel.ngrok-free.app
VITE_API_BASE_URL=
